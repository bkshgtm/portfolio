import React from "react";
import { Section, SectionDivider, SectionTitle } from "../../styles/GlobalComponents";

// Remove the code or leave it empty
const Acomplishments = () => {
  return null; 
};

export default Acomplishments;
